import { Context } from './types';

/**
 * Creates an empty context object for encounter scripts.
 * Provides a place to add context initialization logic later.
 */
export function createContext(): Context {
  return {};
}
