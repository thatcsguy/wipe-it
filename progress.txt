## TEST-001 - admin:spawnChariot duration param

- Added dedicated `admin:spawnChariot` handler accepting `(params?: { duration?: number })`
- Uses `params?.duration ?? 3000` for default
- Client `emitSpawnChariot` updated to accept+forward params
- Validated: 10000ms override shows ~9013ms, default shows ~2000ms remaining
- Files: src/server/index.ts, src/client/admin.ts

## TEST-002 - admin:spawnSpreads duration param

- Added dedicated `admin:spawnSpreads` handler accepting `(params?: { duration?: number })`
- Uses `params?.duration ?? 3000` for default
- Client `emitSpawnSpreads` updated to accept+forward params
- Validated: 10000ms override shows ~9010ms, default shows ~2024ms remaining
- Files: src/server/index.ts, src/client/admin.ts

## TEST-003 - admin:spawnRadialKnockback delay/knockbackDuration params

- Added dedicated `admin:spawnRadialKnockback` handler accepting `(params?: { delay?: number, knockbackDuration?: number })`
- Uses `params?.delay ?? 2000` and `params?.knockbackDuration ?? 500`
- Client `emitSpawnRadialKnockback` updated to accept+forward params
- Validated: 8000ms delay override shows ~7008ms, default shows ~1019ms (2000ms default)
- Files: src/server/index.ts, src/client/admin.ts

## TEST-004 - admin:spawnLinearKnockback delay/knockbackDuration params

- Added dedicated `admin:spawnLinearKnockback` handler accepting `(params?: { delay?: number, knockbackDuration?: number })`
- Uses `params?.delay ?? 2000` and `params?.knockbackDuration ?? 500`
- Client `emitSpawnLinearKnockback` updated to accept+forward params
- Validated: 8000ms delay override shows ~7496ms, default shows ~1700ms (2000ms default)
- Files: src/server/index.ts, src/client/admin.ts

## TEST-005 - admin:spawnPointTethers/spawnPlayerTethers duration params

- Added `admin:spawnPointTethers` handler accepting `(params?: { duration?: number })`
- Added `admin:spawnPlayerTethers` handler accepting `(params?: { duration?: number })`
- Both use `params?.duration ?? 3000` for default
- Client `emitSpawnPointTethers` and `emitSpawnPlayerTethers` updated to accept+forward params
- Validated: 10000ms override shows ~9790ms, default shows ~2795ms remaining
- Files: src/server/index.ts, src/client/admin.ts

## TEST-006 - admin:spawnTower duration param

- Updated `admin:spawnTower` handler to accept `(params?: { duration?: number })`
- Uses `params?.duration ?? 5000` for default
- Client `emitSpawnTower` updated to accept+forward params
- Validated: 15000ms override shows ~14020ms, default shows ~4016ms remaining
- Files: src/server/index.ts, src/client/admin.ts
